import{_ as a,o,c as t,a2 as r}from"./chunks/framework.DCVnL8g5.js";const g=JSON.parse('{"title":"ACID y transaction log","description":"","frontmatter":{},"headers":[],"relativePath":"data-engineering/delta-lake/03-acid-y-transaction-log.md","filePath":"data-engineering/delta-lake/03-acid-y-transaction-log.md","lastUpdated":1789040978000,"isDraft":false}'),s={name:"data-engineering/delta-lake/03-acid-y-transaction-log.md"};function n(i,e,l,c,d,u){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="acid-y-transaction-log" tabindex="-1">ACID y transaction log <a class="header-anchor" href="#acid-y-transaction-log" aria-label="Permalink to &quot;ACID y transaction log&quot;">​</a></h1><p>El transaction log es el núcleo de Delta Lake: cada commit produce una <strong>versión</strong> y un <strong>snapshot</strong> serializable. Los lectores no listan el directorio a ciegas; reconstruyen el estado a partir de <code>_delta_log</code>.</p><p>Documentación: <a href="https://docs.delta.io/latest/concurrency-control.html" target="_blank" rel="noreferrer">control de concurrencia</a>, <a href="https://docs.delta.io/latest/versioning.html" target="_blank" rel="noreferrer">compatibilidad / table features</a>, <a href="https://github.com/delta-io/delta/blob/master/PROTOCOL.md" target="_blank" rel="noreferrer">protocolo</a>.</p><h2 id="escritor-a-escritor-b-lector" tabindex="-1">Escritor A, escritor B, lector <a class="header-anchor" href="#escritor-a-escritor-b-lector" aria-label="Permalink to &quot;Escritor A, escritor B, lector&quot;">​</a></h2><p>Imagina <code>/data/events</code>:</p><ol><li>El <strong>lector</strong> abre la tabla y fija el snapshot 12. Durante su job, A y B commitean 13 y 14. El lector <strong>sigue viendo 12</strong>: aislamiento de snapshot. No ve un mezclado de ficheros de 13 y 14.</li><li>El <strong>escritor A</strong> lee el snapshot 12, escribe data files nuevos (aún no visibles) y, al commitear, comprueba que nadie ha invalidado su supuesto. Si no hay conflicto, el log pasa a 13.</li><li>El <strong>escritor B</strong> hizo lo mismo sobre 12. Al validar, el log ya está en 13. Si B solo <strong>añade</strong> ficheros (append ciego) suele poder commitear 14. Si B <strong>reescribe</strong> ficheros que A ya tocó (<code>UPDATE</code>/<code>DELETE</code>/<code>MERGE</code> sobre el mismo conjunto), el commit <strong>falla</strong> con una excepción de modificación concurrente: la tabla no queda a medias.</li></ol><p>Eso es <strong>optimistic concurrency control</strong>: se trabaja contra un snapshot, se preparan ficheros y se valida al final. No hay un lock de fila estilo OLTP.</p><h2 id="las-cuatro-letras-en-esta-tabla" tabindex="-1">Las cuatro letras, en esta tabla <a class="header-anchor" href="#las-cuatro-letras-en-esta-tabla" aria-label="Permalink to &quot;Las cuatro letras, en esta tabla&quot;">​</a></h2><p><strong>Atomicidad.</strong> Un commit entra entero o no entra. El lector nunca ve “la mitad del MERGE”. Los data files huérfanos de un job que murió antes del commit no forman parte de ningún snapshot.</p><p><strong>Consistencia.</strong> El protocolo rechaza writes que rompen el esquema (capítulo 2), y el snapshot es un conjunto coherente de Add/Remove. Constraints (<code>CHECK</code>) son una table feature aparte: solo aplican si están activas y el writer las entiende.</p><p><strong>Aislamiento.</strong> Lecturas ven un snapshot fijo. Escrituras concurrentes se serializan en el log. El aislamiento efectivo es el que documenta Delta para Spark (serializable entre esas operaciones), no “cualquier motor, cualquier storage”.</p><p><strong>Durabilidad.</strong> El commit es durable cuando el storage ha persistido el JSON del log (y los data files que referencia). Si el object store o el log store no cumplen los requisitos de atomicidad/listado que pide la guía de almacenamiento vigente, esas garantías se degradan. No inventes consistencia de S3/ADLS: sigue la <a href="https://docs.delta.io/latest/delta-storage.html" target="_blank" rel="noreferrer">documentación de storage</a> de tu versión.</p><h2 id="commits-versiones-snapshots" tabindex="-1">Commits, versiones, snapshots <a class="header-anchor" href="#commits-versiones-snapshots" aria-label="Permalink to &quot;Commits, versiones, snapshots&quot;">​</a></h2><p>Cada fichero <code>0000000000000000000N.json</code> es el commit <em>N</em>. Describe acciones: añadir ficheros, quitar ficheros del snapshot, cambiar metadata, protocol, etc. El snapshot <em>N</em> se obtiene rejugando el log (o partiendo de un checkpoint) hasta <em>N</em>.</p><p>Los <strong>checkpoints</strong> (Parquet en <code>_delta_log</code>) compactan prefijos del log para que un lector no abra diez mil JSON. Son un detalle de implementación del protocolo, no un backup.</p><p><code>DESCRIBE HISTORY</code> lista commits (capítulo 5). Leer <code>VERSION AS OF</code> materializa el snapshot. No son lo mismo.</p><h2 id="no-edites-delta-log" tabindex="-1">No edites <code>_delta_log</code> <a class="header-anchor" href="#no-edites-delta-log" aria-label="Permalink to &quot;No edites `_delta_log`&quot;">​</a></h2><p><code>_delta_log</code> es parte interna del protocolo.</p><p>No hagas:</p><ul><li>editar o reescribir JSON de commits;</li><li>borrar un <code>0000….json</code> “porque falló un job”;</li><li>mezclar a mano Parquet de otra tabla en el directorio;</li><li>copiar solo los data files sin el log (o al revés) y tratarlo como la misma tabla.</li></ul><p>Para inspección pedagógica: <code>list</code> del directorio y <code>DESCRIBE HISTORY</code>. La reparación de una tabla corrupta es un incidente: restaura desde backup o desde un snapshot que <strong>aún</strong> exista, no parchees el log.</p><h2 id="conflictos-que-choca-con-que" tabindex="-1">Conflictos (qué choca con qué) <a class="header-anchor" href="#conflictos-que-choca-con-que" aria-label="Permalink to &quot;Conflictos (qué choca con qué)&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>INSERT/append</th><th>UPDATE / DELETE / MERGE</th><th>Compactación (<code>dataChange = false</code>)</th></tr></thead><tbody><tr><td>INSERT/append</td><td>no chocan</td><td>pueden chocar</td><td>no chocan</td></tr><tr><td>UPDATE / DELETE / MERGE</td><td>pueden chocar</td><td>pueden chocar</td><td>pueden chocar</td></tr><tr><td>Compactación</td><td>no chocan</td><td>pueden chocar</td><td>pueden chocar</td></tr></tbody></table><p>“Pueden” depende de si operan sobre <strong>los mismos ficheros</strong>. Particionar por la columna del <code>WHERE</code> y <strong>poner esa columna en la condición</strong> reduce solapes. Particionar por cardinalidad altísima para “evitar conflictos” crea otro problema (capítulo 6).</p><p>Si dos streams usan el <strong>mismo</strong> <code>checkpointLocation</code> a la vez, puedes ver <code>ConcurrentTransactionException</code>. Un checkpoint de streaming no se comparte.</p><h2 id="protocolo-reader-writer-table-features" tabindex="-1">Protocolo: reader, writer, table features <a class="header-anchor" href="#protocolo-reader-writer-table-features" aria-label="Permalink to &quot;Protocolo: reader, writer, table features&quot;">​</a></h2><p>Cada tabla declara un <strong>read protocol</strong> y un <strong>write protocol</strong>. Desde Delta Lake 2.3 las <strong>table features</strong> afinan qué capacidades hay (CDF, deletion vectors, column mapping, clustering, …) en lugar de un número opaco solo.</p><ul><li>Un <strong>reader</strong> que no entiende una feature de lectura <strong>no puede</strong> leer la tabla.</li><li>Un <strong>writer</strong> que no entiende una feature de escritura <strong>no puede</strong> escribirla.</li><li>Activar una feature puede <strong>subir</strong> esos requisitos. Los clientes viejos (otro motor, otro job, un Trino desactualizado) dejan de ser compatibles.</li></ul><p>No actives deletion vectors, column mapping, clustering u otras features “porque sí”. Elige la mínima que necesitas. <code>ALTER TABLE … SET TBLPROPERTIES (&#39;delta.minReaderVersion&#39; = …)</code> existe; usarlo a ciegas es una forma fácil de romper lectores.</p><p>Delta es <strong>compatible hacia atrás</strong> en el sentido habitual: un cliente nuevo lee tablas viejas. <strong>No</strong> es gratis hacia adelante: la tabla nueva puede exigir un cliente nuevo.</p><h2 id="storage-y-commits" tabindex="-1">Storage y commits <a class="header-anchor" href="#storage-y-commits" aria-label="Permalink to &quot;Storage y commits&quot;">​</a></h2><p>Delta se usa sobre S3, ADLS, GCS, HDFS y filesystems locales. El commit necesita que el motor pueda publicar el siguiente JSON del log de forma segura cuando hay varios writers. En algunos despliegues históricos eso implicaba un <strong>LogStore</strong> coordinado; la guía vigente de tu release es la fuente, no recetas de blogs de 2019. Este manual no configura un cloud concreto.</p><p>Siguiente: <a href="./04-merge-updates-y-deletes">MERGE, updates y deletes</a>.</p>',33)])])}const m=a(s,[["render",n]]);export{g as __pageData,m as default};
